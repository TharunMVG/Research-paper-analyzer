This folder contains the ipynb notebooks required to deploy the project. The main notebook that needs to be executed is Paper_analyser.ipynb. The other notebooks contain logic for preprocessing andd building a knowledge graph (enhanced_nlp.py), prompt engineering (prompt_enginnering.py), and fine-tuning(model_finetuning.py).
